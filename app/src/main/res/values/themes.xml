<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.MicroBlogBoticario" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/themedark_black</item>
        <item name="colorPrimaryVariant">@color/themedark_chumbo</item>
        <item name="colorOnPrimary">@color/themedark_chumbo</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/themedark_grey_light</item>
        <item name="colorSecondaryVariant">@color/boticario_background</item>
        <item name="colorOnSecondary">@color/themedark_white</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">@color/black</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="Theme.MicroBlogBoticario.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.MicroBlogBoticario.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.MicroBlogBoticario.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="Theme.MicroBlogBoticario.Fullscreen" parent="Theme.MicroBlogBoticario">
        <item name="android:actionBarStyle">
            @style/Widget.Theme.MicroBlogBoticario.ActionBar.Fullscreen
        </item>
        <item name="android:windowActionBarOverlay">true</item>
        <item name="android:windowBackground">@null</item>
    </style>

    <style name="ThemeOverlay.MicroBlogBoticario.FullscreenContainer" parent="">
        <item name="fullscreenBackgroundColor">@color/themedark_black</item>
        <item name="fullscreenTextColor">@color/colorPrimaryDark</item>
    </style>

    <style name="Theme.AppCompat.Light.NoActionBar.FullScreen" parent="@style/Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowActionBar">false</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>
</resources>